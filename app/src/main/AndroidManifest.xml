<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.junhao.hetty_computer_warehouse_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hetty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hetty_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HETTY_Computer_Warehouse_System.NoActionBar">
        <activity
            android:name=".ui.login.ResetPassword"
            android:exported="true" />
        <activity
            android:name=".ui.login.VerifyCode"
            android:exported="true" />
        <activity
            android:name=".ui.login.RecoverPass"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginPage"
            android:exported="true" />
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="android:screenOrientation"
            android:stateNotNeeded="true"/>
        <activity
            android:name=".ui.home.HomePage"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HETTY_Computer_Warehouse_System.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>