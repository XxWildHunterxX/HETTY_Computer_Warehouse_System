<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.junhao.hetty_computer_warehouse_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HETTY_Computer_Warehouse_System">

        <activity
            android:name=".ui.main.TrackingPage"
            android:exported="true"
            android:label="@string/title_activity_tracking_page"
            android:theme="@style/Theme.HETTY_Computer_Warehouse_System.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>